import { Text, View } from "react-native";

const CalendarScreen = () => {
  return (
    <View>
      <Text style={{ color: "white" }}>CalendarScreen</Text>
    </View>
  );
};

export default CalendarScreen;
